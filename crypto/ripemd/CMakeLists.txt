CYREN_INSTALL_HEADERS(ripemd.h PREFIX openssl)
CYREN_INSTALL_HEADERS(rmd_locl.h rmdconst.h INTERNAL)

OPENSSL_ADD_CRYPTO_SOURCES(
    rmd_dgst.c
    rmd_one.c
    PARENT_SCOPE)

IF(CYREN_ARCH_IS_I386)
    OPENSSL_ADD_CRYPTO_SOURCES(
        asm/rmd-586.pl
        PARENT_SCOPE PERLASM)

    OPENSSL_ADD_CRYPTO_DEFINITIONS(
        -DRMD160_ASM
        PARENT_SCOPE)
ENDIF()

OPENSSL_ADD_CRYPTO_TESTS(
    rmdtest.c
    PARENT_SCOPE)
